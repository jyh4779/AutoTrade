# 퀀트 보완 구현 및 재시작 검증

검증 시각: 2026-09-09 14:30 KST. 아래 수치는 해당 시각의 결과이며 정기 점검 보고서는 계속 갱신된다.

## 적용 확인

- 서비스 AutoTrade_Guardian 실행 중. 스케줄러 PID 16892, 감시 프로세스 PID 19532의 시작 로그에서 현재 소스 버전 적용을 확인했다.
- 적용 소스 SHA-256: `08e3c622f0987e3bc2665abee151cfc156a9eabc1e6b5b4fada23c12ec595e0c`.
- 자체 점검은 5분마다 실행되며 `docs/audit/YYYY-MM-DD.json`과 `.md`에 결과를 기록한다.
- 원본 구조화 이벤트는 `data/audit.db`, 내보낸 이벤트는 `log/events`, 작업별 전체 출력은 `log/jobs`에 보관한다.

## 구현 범위

| 구분 | 구현한 내용 | 검증 범위 |
|---|---|---|
| 데이터·종목 선정 | 전일 확정 일봉과 당일 실제 누적 거래량 분리, 시세 수신 시각·이력 기준일 검사, 후보별 탈락 사유 및 입력 저장 | 실제 시세를 사용한 주문 없는 50종목 검사 |
| 평가·배분 | 공통 점수 수식, 가중치 유효성 검사, 기준 점수와 예산에 따른 배분, 주문 직전 위험 한도 재검사 | 점수 재현 28건 일치, 회귀 테스트 |
| 주문·체결 | 주문 의도 영속화, 중복 방지, 응답 불명 상태 보존, 실제 누적 체결 기준 차분 반영, 취소 확인 | 가짜 브로커로 부분 체결·응답 유실·재시작·취소 검증 |
| 청산·위험 | 계좌 동기화, 가격별 청산 사유, 계좌 손실에 따른 추가 진입 차단, 청산 후 잔여 확인 | 경계값·손실 차단 테스트, 실제 계좌와 장부의 보유 수량 일치 |
| 전략 변경·연구 | 가중치 자동 적용 대신 변경 제안 기록, 저장된 입력 재현, 분봉 청산 시뮬레이션의 불확실성 표시 | 재현 검증 완료, 수익성 검증 미완료 |
| 운영·자체 점검 | 실행 버전, 작업 시작·종료, 데이터 무결성, 독립 점수 재계산, 브로커 수량 대조, 누락 증거 구분 | 정기 점검 실행 및 재시작 적용 확인 |

현재 위험 정책은 종목당 현금 기준 20%, 총 매수 70%, 최소 잔여 현금 10만원, 일일 손실 기준 2%이다. 일일 손실 기준 자본은 당일 처음 저장된 매수 전 현금이므로 일반적인 순자산 기준 손실률과 동일하지 않다. 기본 익절 5%, 손절 -3%이며 수수료·세금 계산의 비용률 0.00198은 기존 가정이다. 이 값들은 수익 최적화가 입증된 매개변수가 아니다.

## 검증 결과

- 회귀 테스트 29개 통과. `compileall`, `git diff --check` 통과.
- 사전 분석 및 주문 없는 전체 매수 판단 작업 종료 코드 0.
- 오후 검증 후보 5개 최고 점수 약 0.584, 기준 0.70. 배분 없음 및 매수하지 않은 사유 기록. 이 결과는 오전 매매 판단을 소급 재현한 결과가 아니다.
- 저장된 기술 점수 재현 28건 PASS. 전체 50종목 중 앞 단계 필터로 탈락한 종목은 기술 점수 재현 대상이 아니다.
- 자체 점검: PASS 560, WARN 50, UNKNOWN 2, N/A 2, FAIL 0.
- WARN 50건은 공급자 시세 시각 부재로 수신 시각만 검증 가능하다는 뜻이다.
- UNKNOWN 2건은 계측 도입 전 오전 작업으로, 현재 로그만으로 과거 정상 수행을 입증할 수 없다. 이 때문에 전체 상태와 정기 작업 종료 코드는 UNKNOWN/3이다.
- 실제 계좌 및 로컬 장부 모두 보유 종목 없음. 검증 작업은 실제 주문을 전송하지 않았다.

## 남은 검증 및 확장

실제 주문·부분 체결·청산은 신규 로직 적용 후 실계좌 사례가 없어 운영 검증 N/A다. 단위 테스트 통과를 실체결 검증 완료로 취급하지 않는다. 다음 정상 예약 실행부터 작업 완료 증거가 쌓여야 한다.

전체 포트폴리오 백테스트, 동일 시각의 전체 코스피 비교군 수익률 수집, 거래 비용·슬리피지 추정, 순차 외부 표본 검증 및 가중치 승격 기준의 성과 검증은 아직 완료되지 않았다. 현재 재현 기능은 판단 입력과 수식의 일치 여부를 확인하는 기반이며, 수익성 또는 배점 개선 효과를 입증하지 않는다. 뉴스 원문 발행 시각도 검증되지 않아 과거 시점 연구에 그대로 사용해서는 안 된다.

수동 점검: `venv2\Scripts\python.exe -m src.audit.run_daily --date YYYY-MM-DD --broker` (브로커 비교는 당일만 가능).

판단 재현: `venv2\Scripts\python.exe -m src.research.replay --date YYYY-MM-DD`.
